@model IEnumerable<OECS.Models.Category>

@if (Model.Count() > 0)
{
    <li class="nav-item active">
        <a class="nav-link" href="#">
            All
            <span class="sr-only">(current)</span>
        </a>
    </li>

    foreach (var category in Model)
    {
        <li class="nav-item">
            <a class="nav-link" href="#" onclick="choose()">@category.category1</a>
        </li>
    }
}
else
{
    <li class="nav-item">
        <a class="nav-link" href="#" onclick="choose()">No Category Available</a>
    </li>
}